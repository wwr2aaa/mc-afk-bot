name: Minecraft AFK Bot
on:
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Download MCC
        run: |
          # Скачиваем последнюю версию напрямую без указания конкретного номера версии
          wget https://github.com/MC-Console-Client/Minecraft-Console-Client/releases/latest/download/MinecraftClient-Linux-zlib-x64
          chmod +x MinecraftClient-Linux-zlib-x64

      - name: Run Minecraft Client
        # Запускаем скачанный файл
        run: ./MinecraftClient-Linux-zlib-x64 "${{ secrets.MC_LOGIN }}" Microsoft "${{ secrets.SERVER_IP }}"
